FROM v2ray/official

# Copy your config.json file into the container
COPY config.json /etc/v2ray/config.json

# Expose the port (80 for WebSocket)
EXPOSE 80

# Command to run the V2Ray service
CMD ["/usr/bin/v2ray", "-config", "/etc/v2ray/config.json"]
