FROM v2ray/official

# Copy your V2Ray config.json
COPY config.json /etc/v2ray/config.json

# Expose the port (in this case, 80 for VMess WebSocket)
EXPOSE 80

# Command to run V2Ray
CMD ["/usr/bin/v2ray", "-config", "/etc/v2ray/config.json"]
